* Windows: `Ctrl-c` att kopiera stöds men använda `Shift-insert` att klistra in.
  * FireFox/IE kanske inte stöder Urklipp behörigheter korrekt.
* Mac OS: `Cmd-c` att kopiera och `Cmd-v` att klistra in.